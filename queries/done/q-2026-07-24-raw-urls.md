---
type: query
id: q-2026-07-24-raw-urls          # results are named to match this id
format: csv                          # csv | md
columns: [url, title, published, file]   # csv only
scope:                               # optional — narrows the search
  places: []
  topics: []
  entities: [starlink]
  lens: []
---
Extract the URL of every source in raw/ that deals with Starlink, one row per
source page.

Purpose: I am preparing an analytical piece on Starlink in Africa and will run a
big external search to find what raw/ is missing. This list is the dedupe set —
any search result whose URL is already on it can be discarded without reading.

- A source qualifies if it tags [[starlink]] (or [[spacex]] and the Starlink-
  specific entities such as [[gambia-starlink-licence]], [[starlink-car]], the
  Airtel/Vodacom partnerships) **or** its verbatim body mentions Starlink —
  tagging is deliberately sparse, so the body grep is what makes the list
  complete.
- Take `url`, `title`, `published` from each source's frontmatter. `file` is the
  source page's filename. Skip paired artefact files (pdf etc.) — their page
  carries the url.
- If a qualifying source has no url (offline artefact, hand-clip), still emit
  the row with url empty.
- No relevance judgement beyond the Starlink test — a passing mention still
  qualifies; the point is dedupe, not curation.
