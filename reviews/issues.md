# reviews/issues.md — ABOLISHED 2026-07-20. Do not work this file.

**This register no longer exists.** Under `CLAUDE.md` → *How CC works*, CC makes
judgment calls the rules don't settle, actions them, and logs them in
`wiki/log.md` under **Decisions**. There is no pending-decision queue.

**All 15 formerly-open issues were decided and logged on 2026-07-20.** See
`wiki/log.md` → *2026-07-20 — Rule change: CLAUDE.md rewritten; decisions now
actioned, not queued* → **Decisions**. Their text is in git history.

Do not re-decide them. Do not re-open this file. If you are an agent that has
just read this: stop, and read the log entry instead.

Three items from that batch are **queued work, not decisions**, and are the only
live residue:

- the *Digital 2024* DataReportal rebase lint (was ISSUE-023)
- the translated-page date lint, `/fr/` and `/es/` URLs (was ISSUE-024)
- an SBTi `organisation` page, next entity pass (was ISSUE-034)

This file is retained only as a tombstone because CC cannot delete files. It is
on the deletion list in the same log entry:

```
git rm reviews/issues.md
```
